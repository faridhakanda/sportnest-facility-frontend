import Link from 'next/link';
import React from 'react';
import Navbar from './navbar';

const Header = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Header;